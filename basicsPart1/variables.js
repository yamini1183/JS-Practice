const studentId = 101 //cannot be changed later
let studentName = "Yamini" //can be altered 
var studentContact = 9324355224 // can be altered later but is no longer used as it has no block scope 

console.log(studentId)
console.table([studentId, studentName, studentContact])

